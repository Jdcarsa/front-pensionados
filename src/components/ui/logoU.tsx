export default function LogoU({ width, height }: { width: number; height: number }) {
    return (
        <img
            src="/logoUniversidad.svg"
            alt="Logo"
            height={height}
            width={width}
        />
    );
}
